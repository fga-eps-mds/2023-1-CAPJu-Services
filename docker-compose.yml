version: "3.3"

services:
  user:
    build:
      context: ./
      dockerfile: Dockerfile
    container_name: user
    command:
      [
        "npm",
        "run",
        "start",
      ]
    restart: always
    volumes:
      - ./:/app
    ports:
      - 8080:8080
    networks:
      - 2023-2-capju-config_capju
    env_file:
      - .env

networks:
  2023-2-capju-config_capju:
    external: true
