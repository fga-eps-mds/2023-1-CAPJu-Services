version: "3.3"

services:
  process_management:
    build:
      context: ./
      dockerfile: Dockerfile
    container_name: process_management
    command:
      [
        "npm",
        "run",
        "start",
      ]
    restart: always
    volumes:
      - ./:/app
    ports:
      - 8082:8082
    networks:
      - 2023-2-capju-config_capju
    env_file:
      - .env

networks:
  2023-2-capju-config_capju:
    external: true
